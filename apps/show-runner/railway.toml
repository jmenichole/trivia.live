[build]
builder = "NIXPACKS"
watchPatterns = ["apps/show-runner/**", "packages/game-engine/**"]
buildCommand = "cd ../.. && npm install && npm run build -w @trivia-live/show-runner"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
